# 验证范围（v0.1.0）

验证环境：Windows、本机 R 4.5.2，UTF-8 进程环境。示例数据全部为合成数据。

## 已执行

- 三个 Skill 的标准结构验证：SKILL.md frontmatter、名称和脚手架检查通过。
- UI元数据、内部参考链接与脚本路径检查通过。
- 三个 demo.R 离线运行通过；输出标准项目结构、派生CSV/RDS与验证记录。
- 地址：前导零ID、重复地址回填、空地址、重复/未知响应、坐标系和数值范围验证。
- 对账：两侧差异、编辑距离候选、空键不匹配、B重复键阻止连接、显式审核应用、匹配与指标缺失分开、全缺失求和保留NA。
- 统计：严格数值转换、编码和标签往返、百分比分母、全缺失/单值/常量连续变量。
- 独立代理另执行32个离线探针；发现审核冲突、数值汇总溢出、无水平因子丢失缺失信息、转换错误定位不足，已加入回归断言并修正。
- 发布文件不含模板原文中的API Key、个人绝对路径或真实健康数据；检查常见密钥模式。

## 未验证/边界

- 没有调用真实地图API，没有验证账户额度、联网请求或坐标转换精度。
- 普通表辅助函数测试不代表sf/GPKG完整工作流已实测。
- 描述统计辅助函数测试不代表Word分页、回归模型或实际医学项目已经验收。
- 三个真实需求情景只做了工作流推演；未知事件定义、模糊归属或地理上下文仍需用户明确。

## 复现

在仓库根目录执行README的三个Rscript命令；为每次运行选择新目标目录。示例的非零退出码表示断言或环境检查未通过。辅助脚本正常执行不代表用户数据正确；实际项目仍需按Skill中的输出核验步骤验收。
